export const enum senderEnum {
	Customer,
	User
}

export const enum openStrategyEnum {
	ModalCenter = "ModalCenter",
	ModalRight = "ModalRight",
	NewWindow = "NewWindow",
	CurrentTab = "CurrentTab"
}